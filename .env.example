# Environment Variables for Vercel Postgres

# Required for Vercel Postgres connection
# These will be automatically provided by Vercel when you add a Postgres database
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Optional: If you want to test locally
# Create a .env.local file with your local database connection string
# DATABASE_URL=postgresql://username:password@localhost:5432/flashcard_db

# Server configuration
PORT=3000
